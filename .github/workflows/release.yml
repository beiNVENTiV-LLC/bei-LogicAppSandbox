# ---------------------------------------------------------------------------
# bei-LogicAppSandbox - cut a release
#
# STATUS: PLACEHOLDER. MODULE 4 IS NOT IMPLEMENTED.
#
# This workflow deliberately fails if it is invoked. It exists so that the
# deployment surface is visible and reviewable, not so that it can run.
#
# It contains, by design:
#   - no Azure login and no OIDC token request
#   - no deployment step
#   - no artifact upload, download or publication
#   - no Azure credential, subscription id, tenant id, client id, resource
#     group name or resource name
#
# It is manual-only (workflow_dispatch); no push, pull_request, schedule or
# release event can start it.
#
# Implementing this workflow is MODULE 4 work and is currently
# BLOCKED - AZURE IDENTITY REQUIRED. See:
#   docs/decisions/0001-oidc-deployment-identity-blocked.md
# ---------------------------------------------------------------------------
name: release

on:
  workflow_dispatch:

permissions:
  contents: read

defaults:
  run:
    shell: bash

jobs:
  not-implemented:
    name: not-implemented
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Explain why this workflow cannot run
        run: |
          {
            echo "### release - NOT IMPLEMENTED"
            echo ""
            echo "MODULE 4 configuration is required before this workflow can run."
            echo ""
            echo "**Intended future purpose**"
            echo ""
            echo "- Verify VERSION matches the semantic version being released"
            echo "- Verify CHANGELOG.md has a matching, non-empty section"
            echo "- Create the annotated tag v&lt;version&gt; on the accepted main commit"
            echo "- Create the GitHub Release with the commit SHA, merged pull request, validation result, known blockers and next module"
            echo "- Never move or reuse a published tag"
            echo ""
            echo "**Blocked on**"
            echo ""
            echo "- MODULE 4 release policy sign-off"
            echo "- A decision on whether release creation is automated or performed by an authorised maintainer"
            echo "- Confirmation of which GitHub token permissions the release job may hold"
            echo ""
            echo "See docs/decisions/0001-oidc-deployment-identity-blocked.md."
          } >> "$GITHUB_STEP_SUMMARY"

      - name: Fail deliberately
        run: |
          echo "::error::release is a MODULE 2 placeholder. MODULE 4 configuration is required before this workflow can run. Nothing was deployed."
          exit 1
